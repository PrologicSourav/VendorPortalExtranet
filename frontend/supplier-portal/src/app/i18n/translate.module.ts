// AppTranslateModule is no longer needed — translation providers
// are now configured directly in appConfig (app.config.ts).
// This file can be deleted once all references to AppTranslateModule
// have been removed.
